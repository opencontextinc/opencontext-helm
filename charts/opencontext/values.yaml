# Default values for opencontext.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: "us-docker.pkg.dev/vpc-host-prod-345521/oc-docker/opencontext"
  pullPolicy: Always
  tag: latest

imagePullSecrets:
  - name: opencontext-artifact-registry
nameOverride: ""
fullnameOverride: ""

serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""

podAnnotations: {}

podSecurityContext:
  runAsUser: 1000
  runAsGroup: 1000
  fsGroup: 1000
  fsGroupChangePolicy: "OnRootMismatch"

securityContext:
  allowPrivilegeEscalation: false
  runAsNonRoot: true
  runAsUser: 1000
  runAsGroup: 1000

livenessProbe:
  tcpSocket:
    port: 7007
  failureThreshold: 1
  periodSeconds: 10

startupProbe:
  tcpSocket:
    port: 7007
  failureThreshold: 30
  periodSeconds: 10

service:
  type: ClusterIP
  port: 7007

ingress:
  enabled: false
  className:
  annotations:
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
    # cert-manager.io/cluster-issuer: selfsigned-issuer
  hosts: []
    # Make sure to change app.url to match the host set
  tls: []

issuer:
  email:
  clusterIssuer: selfsigned-issuer

resources: {}
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  # limits:
  #   cpu: 100m
  #   memory: 128Mi
  # requests:
  #   cpu: 100m
  #   memory: 128Mi

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 100
  targetCPUUtilizationPercentage: 80
  # targetMemoryUtilizationPercentage: 80

nodeSelector: {}

tolerations: []

affinity: {}

#
# Values for the PostgreSQL subchart from
# https://artifacthub.io/packages/helm/bitnami/postgresql
#

# Global values
global:
  postgresql:
    auth:
      username: opencontext
      existingSecret: opencontext-postgresql
    caFilename: ca.crt
  nodeSelector: {}

# PostgreSQL subchart
postgresql:
  enabled: true
  nameOverride: opencontext-postgresql
  commonLabels:
    app.kubernetes.io/part-of: opencontext-server
  primary:
    initdb:
      scriptsSecret: opencontext-postgresql-initdb
    persistence:
      enabled: true
      size: 10Gi
  tls:
    enabled: true
    certificatesSecret: opencontext-postgresql-certs
    certFilename: tls.crt
    certKeyFilename: tls.key
  volumePermissions:
    enabled: true
  service:
    port: 5432

# Values for OpenContext
app:
  orgName:
  # If Ingress is disabled use kubectl port-forward to forward the OpenContext service to port 8080
  # You will then be able to access it at http://localhost:8080
  url: http://localhost:8080
  database:
    prefix: opencontext_
    connection:
      host:
      port:
      user:
      password:
      ssl:
        enabled: true
        # If true, the server certificate is verified against the list of supplied CAs.
        # An error event is emitted if verification fails
        rejectUnauthorized: false
      pool:
        min: 0
        max: 20
  github:
    # token or app
    authType: token
    token: MY_TOKEN
    host:
    appAuth:
      appAuthSecret: opencontext-github-app-auth
      createAppAuthFromFile:
  bitbucket:
    enabled: false
    # appPassword or token
    authType: appPassword
    token:
    host:
    appAuth:
      username:
      appPassword:
  pagerDuty:
    enabled: false
    token:
  googleCloudStorage:
    enabled: false
    useServiceAccount: false
    serviceAccountSecret: opencontext-google-cloud-storage
    createServiceAccountFromFile:
  auth:
    # Change to true to use Google OAuth for authentication
    enabled: false
    provider:
      google:
        clientId:
        clientSecret:
  catalog:
    intervalSecs: 200
    locations:
      githubOrg: []
    #    # Discover people and teams in GitHub org
    #    # URL Format: https://github.com/${MY_ORG}
    #    - https://github.com/scatter-ly
      githubDiscovery: []
    #    # Discover repos, codepath and other assoicated artifacts in a GitHub repo
    #    # URL Format: https://github.com/${MY_ORG}/${MY_REPO}/
    #    - https://github.com/scatter-ly/publictest/
    #    # Discover all OpenContext YAML files in a specific GitHub repo
    #    # URL Format: https://github.com/${MY_ORG}/${MY_REPO}/blob/${MY_BRANCH}/*.yaml
    #    - https://github.com/scatter-ly/scatter.ly/blob/main/*.yaml
    #  url:
    #    # Discover OpenContext catalog YAML file in a public GitHub repository
    #    - https://github.com/scatter-ly/publictest/blob/main/oc-catalog.yaml
    #    # Discover all OpenContext YAML files in a specific BitBucket repo
    #    # URL Format: https://bitbucket.org/${WORKSPACE_ID}/${MY_REPO}/src/${MY_BRANCH}/*.yaml
    #    - https://bitbucket.org/scatter-ly/scatter.ly/src/main/*.yaml
    #  gcsDiscovery:
    #    # Discover all OpenContext YAML files in a Google Cloud Storage bucket
    #    # URL Format: https://storage.cloud.google.com/${GCS_BUCKET}/${GCS_BUCKET_PATH_TO_YAML}/*
    #    - https://storage.cloud.google.com/${GCS_BUCKET}/${GCS_BUCKET_PATH_TO_YAML}/*
    #  bitbucketDiscovery:
    #    # Workspaces should be referenced using the workspace ID and projects should use the project key.
    #    # Discover all repositories in a workspace
    #    # URL Format: https://bitbucket.org/workspaces/${WORKSPACE_ID}
    #    - https://bitbucket.org/workspaces/${WORKSPACE_ID}
    #    # Discover all repositories in a project
    #    # URL Format: https://bitbucket.org/workspaces/${WORKSPACE_ID}/projects/${PROJECT_KEY}
    #    - https://bitbucket.org/workspaces/${WORKSPACE_ID}/projects/${PROJECT_KEY}
    #    # Select only a specific repository
    #    # URL Format: https://bitbucket.org/workspaces/${WORKSPACE_ID}/${MY_REPO}
    #    - https://bitbucket.org/workspaces/${WORKSPACE_ID}/${MY_REPO}
    #    # Search for OpenContext catalog YAML file in all repositories in a project
    #    # URL Format: https://bitbucket.org/workspaces/${WORKSPACE_ID}/projects/${PROJECT_KEY}/repos/*?search=true&catalogPath=my/nested/path/catalog.yaml
    #    - https://bitbucket.org/workspaces/${WORKSPACE_ID}/projects/${PROJECT_KEY}/repos/*?search=true&catalogPath=my/nested/path/catalog.yaml
